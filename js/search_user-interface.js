$(function(){
  var user_name = 'jsween';
  $('form#search-user').submit(function(event) {
    event.preventDefault();

    // var user_name = $('#user_name').val(); //move into submit
    // console.log(user_name);
    // var user_data = getRepos(apiKey, user_name);
    // console.log('user data is ' + user_data);
    // var new_user = new User(data);
    //console.log('The new user object is: ' + new_user);
  }); //end of submit form
  // var new_user1 = getRepos(apiKey, user_name);
  // console.log('response is ' + new_user1);
}); //end of when ready
